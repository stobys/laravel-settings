<?php
return [
    // -- Cache Filename
    'cache_file' => storage_path('settings.json'),

    // -- Table name to store settings
    'db_table'   => 'settings',

    // -- Fallback setting
    'fallback'   => true
];